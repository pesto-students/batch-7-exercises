import { isTSAnyKeyword } from "@babel/types";

describe('addingVectorGraphicsToWeb', ()=> {
    it ('should Create a html file and add both the .png and .svg star image in the document', ()=>{
        expect(true).toBe(true);
    })
})