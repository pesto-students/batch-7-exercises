describe('client test', () => {
  it('should render an input box to add new todos', () => {});

  it('should render a list of todos', () => {});

  it('should render an Add Task button', () => {});

  it('should render a new page on Add Task button', () => {});

  it('should render an delete button against every task', () => {});

  it('should render an edit button against every task', () => {})

  it('should change the todo item to input box on clicking edit button', () => {})
});
