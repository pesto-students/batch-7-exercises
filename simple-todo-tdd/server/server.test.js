describe('server tests', () => {
  it('should check if server is up', async () => {
    // check for server response code 200
  });

  it('should check if connected to database', () => {});

  it('should get all todos', () => {});

  it('should get a particular todo by id', () => {});

  it('should add a new todo', () => {});

  it('should delete a new todo by id', () => {});

  it('should edit a todo by id', () => {});

  it('should complete a todo by id', () => {});

  it('should undo completion of todo by id', () => {});
});
