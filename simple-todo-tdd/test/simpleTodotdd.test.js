describe("Client Functionality", () => {
  test("adding todo adds up in the new todo list", () => {
    expect(true).toBe(true);
  });
  test("new todo length should not be greater 256", () => {
    expect(true).toBe(true);
  });
  test("throws error incase of null", () => {
    expect(true).toBe(true);
  });
  test("should remove the todo from todo list", () => {
    expect(true).toBe(true);
  });
  test("todo value should be updated on edit click", () => {
    expect(true).toBe(true);
  });
});
